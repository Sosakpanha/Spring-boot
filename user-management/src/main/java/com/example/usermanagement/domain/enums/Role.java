package com.example.usermanagement.domain.enums;

/**
 * Domain enum representing user roles for authorization.
 *
 * Part of the DOMAIN LAYER - no external dependencies.
 *
 * Roles:
 * - USER: Standard user with basic access
 * - ADMIN: Administrator with full access
 */
public enum Role {
    USER,
    ADMIN
}
